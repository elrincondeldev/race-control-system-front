import { COGNITO_REGION, COGNITO_CLIENT_ID, COGNITO_USER_POOL_ID } from '../constants';

export const cognitoConfig = {
    region: COGNITO_REGION,
    userPoolId: COGNITO_USER_POOL_ID,
    clientId: COGNITO_CLIENT_ID
}